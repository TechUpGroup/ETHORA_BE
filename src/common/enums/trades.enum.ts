export enum TRADE_STATE {
  QUEUED = "QUEUED",
  CREATED = "CREATED",
  OPENED = "OPENED",
  CLOSED = "CLOSED",
  CANCELLED = "CANCELLED",
}

export enum TRADE_TOKEN {
  USDC = "USDC",
}

export enum TRADE_STATUS {
  WIN = "Win",
  LOSS = "Loss",
}
